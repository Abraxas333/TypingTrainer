# ============================================================================
# requirements.txt  —  DEPENDENCY REFERENCE for typing-terminal
# ============================================================================
#
# NOTE: This is NOT a Python project, so this file is a human-readable
# reference only. It is NOT consumed by pip. The authoritative dependency
# sources are package.json (JavaScript) and src-tauri/Cargo.toml (Rust).
#
# To actually install everything automatically:
#
#     npm install            # installs all JS dependencies from package.json
#     # Rust/Cargo deps are resolved automatically on the first Tauri build:
#     npm run tauri dev      # (or: npm run tauri build)
#
# Prerequisites that must already be on the system:
#     - Node.js (LTS)        https://nodejs.org/
#     - Rust toolchain       https://www.rust-lang.org/tools/install
#     - Tauri system deps    https://tauri.app/start/prerequisites/
# ============================================================================

# --- JavaScript runtime dependencies (from package.json "dependencies") ---
@tauri-apps/api>=2.0.0
@tauri-apps/plugin-fs>=2.5.1
@tauri-apps/plugin-opener>=2.0.0
highlight.js>=11.11.1
xterm>=5.3.0

# --- JavaScript dev dependencies (from package.json "devDependencies") ---
@tauri-apps/cli>=2.0.0
typescript~=5.6.2
vite>=6.0.3

# --- Rust crates (from src-tauri/Cargo.toml; installed via Cargo) ---
# tauri = "2"
# tauri-plugin-fs = "2"
# tauri-plugin-opener = "2"
# serde / serde_json (typical Tauri deps)
